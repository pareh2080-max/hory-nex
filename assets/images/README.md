# Images

Placez ici :
- `logo.png` (1024×1024) pour l'icône de l'application et le splash.

Le logo affiché à l'écran est actuellement dessiné en code (voir `lib/ui/widgets/hory_logo.dart`),
aucune image n'est donc obligatoire pour que l'app fonctionne.
